services: 
  db: 
    image: 'postgres:18'

    container_name: backlog_db
    
    ports: 
      - 5432:5432
        
    env_file: 
      - .env

    volumes:
      - ${PWD}/data-db/:/var/lib/postgresql/
